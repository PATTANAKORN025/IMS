{
  "name": "factory-twin-nextjs-spike",
  "version": "0.0.0",
  "private": true,
  "description": "DISPOSABLE Step 1.5 spike only. Not part of the production build, not referenced by any Dockerfile, not wired into docker-compose or CI. Answers exactly two questions: (1) which Next.js major to target, (2) does /factory-twin-3d/'s existing auth_request + proxy_pass shape work with Next.js's basePath model. Delete this whole directory when the spike is done reading -- nothing outside it depends on it.",
  "scripts": {
    "dev": "next dev -p 4300",
    "build": "next build",
    "start": "next start -p 4300"
  },
  "dependencies": {
    "next": "16.3.4",
    "react": "19.3.0",
    "react-dom": "19.3.0"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "@types/node": "^22.10.5",
    "@types/react": "^19.0.2",
    "@types/react-dom": "^19.0.2"
  }
}
