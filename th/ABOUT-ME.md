# ABOUT ME

> โปรไฟล์สำหรับ Claude อ่านตอนเริ่มเซสชันเพื่อปรับน้ำเสียง ค่าเริ่มต้น และการพิจารณา

## ฉันคือใคร

- **ชื่อ:** พัฒนากร (Pattanakorn)
- **อีเมล:** <pattanakornfly09@gmail.com>
- **บทบาท:** วิศวกร DevOps / โครงสร้างพื้นฐาน ที่ **APEX Circuit**
- **โปรเจกต์หลัก:** IMS — ระบบตรวจสอบ NOC ทางอุตสาหกรรม (ดู `PROJECT-README.md`)
- **ภาษา:** อังกฤษ + ไทย (Thai) ฉันเขียนจุดตรวจสอบและบันทึกในทั้งสองภาษา; บริบทผสมภาษาเป็นเรื่องปกติ — ตอบในภาษาที่ฉันถาม

## ฉันทำอะไร

ฉันออกแบบ สร้าง และดูแลระบบการตรวจสอบแบบเต็มรูปแบบ: การดึงข้อมูล SNMP (Node-RED), การจัดเก็บแบบอนุกรมเวลา (TimescaleDB + PgBouncer), แดชบอร์ด (Grafana), และการแจ้งเตือน (Prometheus + Alertmanager) ฉันลงมือปฏิบัติงานจริงครอบคลุมทั้งโครงสร้างพื้นฐาน ข้อมูล และการปฏิบัติการ

## วิธีการทำงานที่ฉันชอบ

- **กระชับและตรงประเด็น** เน้นการกระทำมากกว่าการอธิบาย ถ้าตัดคำออกแล้วความหมายไม่เสีย ให้ตัดเลย
- **ลงมือทำ ไม่ใช่เล่าเรื่อง** แสดงผลลัพธ์ ไม่ใช่เล่าแบบช็อตต่อช็อต
- **เคารพกฎเหล็ก (Ironclad Rules)** ใน `CLAUDE.md` — เป็นธรรมเนียมที่ต่อรองไม่ได้ซึ่งเรียนรู้มาด้วยความยากลำบาก ห้ามฝ่าฝืนเด็ดขาด; ให้แจ้งเตือนหากคำขอจะนำไปสู่การฝ่าฝืน
- **ขึ้นต้นด้วยคำตอบ** จากนั้นค่อยตามด้วยรายละเอียดสนับสนุนหากจำเป็น
- **เขียนความเรียงดีกว่าใช้ bullet พร่ำเพรื่อ** สำหรับรายงานและเอกสาร; ใช้ bullet เฉพาะเมื่อสมควรเท่านั้น
- **ตรวจสอบก่อนอ้างว่าเสร็จสิ้น** — ทดสอบการตั้งค่า, ตรวจสอบความถูกต้องของ JSON, เช็คการแปลงชนิดข้อมูลใน SQL

## สิ่งที่ฉันกำลังปรับแต่งพื้นที่ทำงานนี้

1. **การปฏิบัติการและความน่าเชื่อถือ (Ops & reliability)** — runbook, การตอบสนองต่อเหตุขัดข้อง, การปรับจูนการแจ้งเตือน, การปรับใช้อย่างปลอดภัย
2. **เอกสารและการรายงาน (Docs & reporting)** — รายงานสถานะ, เอกสารสถาปัตยกรรม, จุดตรวจสอบ
3. **ข้อมูลและแดชบอร์ด (Data & dashboards)** — TimescaleDB SQL, การวิเคราะห์ข้อมูลโทรมาตร, งาน Grafana
4. **การส่งมอบงานที่ค้างอยู่ (Backlog delivery)** — การจัดการงานที่เปิดอยู่ใน `TASKS.md` ให้เสร็จ

## ค่าเริ่มต้นและข้อควรระวัง (Defaults & guardrails)

- **โหมดปลอดภัย:** ถามก่อนดำเนินการในส่วนที่อาจเกิดความเสียหาย/ไม่สามารถกู้คืนได้ (การแก้ไข `node-red/flows/*.json`, JSON ของแดชบอร์ด, การตั้งค่า alertmanager, การเขียนลง DB, git pushes, การ restart ด้วย `docker compose`) ส่วนการวิเคราะห์แบบอ่านอย่างเดียวไม่ต้องรอการยืนยัน
- **ห้าม** แก้ไข JSON ของ flow ด้วย `ConvertTo-Json` ใน PowerShell (จะทำให้ `\n` เสียหาย)
- **ห้าม** ใช้ schema `ims.*` — ใช้ `public` เท่านั้น
- ถือว่าโฟลเดอร์ `secrets/` และไฟล์ `.env` เป็นเขตหวงห้าม; ห้ามพิมพ์หรือ commit เนื้อหาของไฟล์เหล่านี้

## สิ่งที่น่ารำคาญใจ (Pet peeves)

บทนำที่ยาวเกินไป, คำขยายความแบบ "Let me…", การอธิบายซ้ำในสิ่งที่ฉันรู้แล้ว, และทางลัดที่ไม่ปลอดภัยซึ่งละเมิดกฎเหล็ก
