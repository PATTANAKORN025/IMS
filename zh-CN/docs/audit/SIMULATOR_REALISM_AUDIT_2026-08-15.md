<!-- GLOBAL_NAV -->
<div align="right">
  <a href="../../README.md"><img src="../../../docs/assets/icons/home.svg" width="16" align="center" /> <b>主页</b></a> &nbsp;|&nbsp;
  <a href="../README.md"><img src="../../../docs/assets/icons/book.svg" width="16" align="center" /> <b>文档索引</b></a>
</div>
<br/>

# 模拟器真实性审计 (P1, 可靠性测试套件)

> 根据当前实时数据集全面重新运行 `LDI_ALARM_FIDELITY_AUDIT.md` (2026-08-11) 的方法。下面所有的数字都是全新的实时查询结果，而不是重新引用 2026-08-11 的数字。分类使用了以下分类法：固定配方/设定点、正常变动、受控漂移、瞬态故障、罕见故障、模拟器伪影、实际缺陷。
>
> 数据集窗口: 2026-08-13T08:22:54Z → 2026-08-15T04:52:29Z, 41,153 行 `ldi_data`, 787 行 `ldi_alarm_log`。

## 头条发现：原始审计中的首要问题已解决，但直到现在才被记录为已解决

| 参数 | 2026-08-11 越线 (OOS) 率 | 2026-08-15 越线 (OOS) 率 | 变化 |
| --- | --- | --- | --- |
| 真空度 (`air_vacuum`) | 26.99% | **0.02%** | 已修复 |
| 温度 | (合并入 23.26% 的环境数据中) | **0.02%** | 已修复 |
| 湿度 | (合并入 23.26% 的环境数据中) | **9.85%** | 部分修复 |
| PE/JE (`abs(pe_1)>10 OR abs(je_1)>10`) | 44.93% | **0.68%** | 已修复 |

**下游效应，也经过了最新验证**：最初审计中的第二个发现（“91.4% 的告警是由条件驱动的，这与真实工厂以噪声为主导的分布情况恰恰相反”）也已反转。由条件驱动的代码现在占所有告警的 **19.3%** (152/787)，而噪声代码占 **80.7%** — 这是现实的、以噪声为主导的状况，与最初审计中提到的缺失的形状完全一致。这并非独立修复的结果 -- 而是遥测数据重新校准所带来的机械性后果：状态驱动的告警由超出规格的遥测数据触发，而现在遥测数据已不再是长期地超出规格。

这两项修复都未在当前仓库的 Git 历史中以“遥测校准”或类似的提交信息进行记录（没有进行穷尽搜索，但在当前会话接触到的规范/审计文档中未找到） -- 这项改善的发生早于本次会话跟踪的工作内容，并且之前未经记录。在此记录将填补这一空白。

## 逐参数分类

| 参数 | 证据 | 分类 |
| --- | --- | --- |
| `scan_speed` | 范围在 101.8-519.5 之间，在 41,153 行数据中**仅有 100 个不同的值** -- 一个离散的/量化的集合，而不是持续的噪声 | **固定配方/设定点** -- 与每个配方的标准扫描速度预设值一致，而非随机生成 |
| `air_vacuum` | OOS 为 0.02% (过去为 26.99%) | **正常的工艺变动** -- 良好校准 |
| 温度 | OOS 为 0.02% | **正常的工艺变动** -- 良好校准 |
| 湿度 | **OOS 为 9.85%**，而且在不同机器上不均匀 (见下方机器变动部分) | **模拟器伪影，尚未重新校准** -- 真空度/温度/PE/JE 均被修复至接近零的 OOS；湿度显然是在那次修复其余参数的重新校准过程中被遗漏的唯一参数 |
| PE (`pe_1`-`pe_6`) / JE (`je_1`-`je_4`) | 相对于每一行自身的 `pe_setting`/`je_setting`，OOS 为 0.68% (按照 `LDI_SPC_GUIDE.md` 记录的每个配方的公差模型，而不是一个固定的全局阈值) | **正常的工艺变动** -- 良好校准 |
| `pe_setting`/`je_setting` | 分别有 5/4 个不同的值 (PE 为 25, 30, 50, 75；JE 为 25, 30, 40, 50)，外加一个异常值 `pe_setting ≈ 1.8`。已检查该异常值是否是受损的/哨兵值 (`WHERE pe_setting > 1000` → 0 行) -- 它确实是一个很小的实数，而不是浮点数最大值的错误读取 | **固定配方/设定点** (4 个整数值) **+ 一个窄公差配方** (~1.8 的值) -- 不是缺陷，通过直接排除损坏假设来验证，而不是想当然 |
| `thickness` | 范围在 0.26mm-2.317mm，1,733 个不同的值 (连续的) | **本轮未分类** -- 在 `ALARM_SEVERITY_GUIDE.md` 或其他地方没有找到记录在案的厚度公差/规格限制来据此对 OOS 率进行分类。这是个真实的空白，我们不应凭空猜测 |
| 功率 (`ldi_metrics.power_watt`)，振动 (`ldi_metrics.vibration`) | 经由 grep 确认: 仅被 `ims-ingestion-latency`、`ims-engineering-drilldown`、`ims-noc-overview` (基础设施/管道健康仪表板) 引用，从未被任何制造/质量仪表板引用 | **降低优先级，未进行同等深度的审计。** 这些值目前不驱动任何工艺决策 (下游没有进行读取以达到质量/RCA 目的) -- 它们的存在是为了证明类似 LDI 这种形式的遥测管道自身是处于运作状态的，而不是用于表现机器的实际物理情况。耗费精力去审计这些当前无人响应的信号不够现实 |
| 告警频率 (噪声与条件驱动的混合比例) | 80.7% 噪声 / 19.3% 条件驱动 (过去为 8.6%/91.4%) | **已修复** -- 确认为上述遥测数据修复的下游结果，并非独立修改 |
| 告警严重性 | 目录中现在存在 Critical 严重性的告警 (43 行)，并且已经在实时数据中触发 (已在 `READ_ONLY_AUDIT_2026-08-15.md` 中确认，本轮中没有重新验证) | **已修复** (经过交叉对比验证，无需重新测量) |
| 告警恢复 (解决后又复发的模式) | **本轮未审计** | 实实在在的空缺 -- 需要在这里未能构建的特定查询模式 (某告警代码在被标记为 RESOLVED 后是否立即再次发生，这意味着底层条件实际上未被清除) |
| 机器间差异 | 真实且已被测量：湿度越限 (OOS) 在 `LDI-01`/`LDI-02`/`LDI-05` 分别为 12.73%/21.19%/24.44%，但在 `LDI-03`/`LDI-04`/`LDI-06`/`LDI-07`/`LDI-08`/`LDI-09`/`LDI-10` 中约等于 0%。另外还注意到：`LDI-03`/`LDI-04` 行数完全一样 (各 4,488 行)，`LDI-07`/`LDI-08` (各 1,514 行) 和 `LDI-09`/`LDI-10` (各 692 行) 也是一样。 | **部分属于受控漂移** -- 湿度的分布情况对应着实际的工艺类型分组（与 `ldi-data-readiness` 覆盖范围矩阵中已经记录的 DF INNER/DF OUTER/SM 流程差异相符），这是现实中的机器间差异，并非随机生成。上述一对对机器中行数完全一致的现象值得在未来环节仔细探究 -- 既有可能是故意设计的“机器孪生”模拟（同产线两台相同的工位），也有可能意味着这两组机器并非在独立发生变动。此处不作决断，只负责客观陈述而非臆断。 |
| 时间相关性 (生产状态 vs. 遥测数据) | `state=false` (未运行) 在 41,184 行中仅有 10 行 -- 样本太小，无法得出任何有关依赖状态的遥测行为的结论 | **样本不足** -- 不是“不存在相关性”，而是确实无法从现有数据中判断 |

## 本次审计未涵盖的内容

- 没有完整地重跑原始 `LDI_ALARM_FIDELITY_AUDIT.md` 的所有 10 个部分（爆发/泛洪行为，`related_log_id`/`match_type` 相关语义，前 30 个严重故障代码合理化分析，跨仪表板一致性，颜色标记一致性）——这些发现要么在本次环节的其他地方已经得到了独立的重新验证（防抖: `SPEC_ALARM_HYGIENE_COMPLETION.md`；严重故障: `READ_ONLY_AUDIT_2026-08-15.md`；仪表板命名: 已在提交 `f39afa9` 中修复），要么没有被重新检查。本次审查着眼于请求的具体参数列表（scan_speed、vacuum、thickness、PE、JE、temperature、humidity、power、vibration、alarms、severity、machine variation、temporal correlation），而不是从头开始做全面的审计重跑。
- 在本轮中未修改任何模拟器代码或配置 -- 仅作审计，依照上述的分类交付物。唯一一项真实、可操作的发现 (湿度的重新校准) 是个候选修复项，暂不在此应用。
- 未去深究_为何_真空度/温度/PE/JE 被修好了但湿度没有 -- 那个重新校准了其它数值的修改没有被识别出来 (粗略搜索下没有找到对应的提交记录)。如果修复湿度被排上了优先级，那这是值得去跟进的，这样同样的机制就能被复用/扩展，而不用重头来过。

## 建议的下一步动作 (这里未做)

使用与 vacuum/temp/PE/JE 相同的手段重新校准湿度的噪声带或越线 (OOS) 阈值，并遵循此项目在历次修复中所一直沿用的严谨原则：“任何对模拟器的修改都必须具备基线证据、根本原因分析、物理/过程原理解释、预期表现以及修改前后的对比证明”。鉴于这种特定于机器的模式 (10 台机器中有 3 台受影响)，修复措施很可能需要针对这 3 台机器共享的那种特定的流程轮廓，而不是去套用单一的全局常量。
