# ABOUT ME

> Claude 的个人档案。在会话开始时阅读，以调整语气、默认设置和判断力。

## 我是谁

- **姓名:** Pattanakorn
- **电子邮件:** <pattanakornfly09@gmail.com>
- **角色:** **APEX Circuit** 的 DevOps / 基础架构工程师
- **主要项目:** IMS — 工业 NOC 监控系统 (请参阅 `PROJECT-README.md`)
- **语言:** 英语 + 泰语 (ไทย)。我用这两种语言编写检查点和笔记；混合语言语境很正常 — 请用我提问的语言回答。

## 我做什么

我设计、构建并运营全栈监控系统：SNMP 轮询 (Node-RED)、时间序列存储 (TimescaleDB + PgBouncer)、仪表板 (Grafana) 和告警 (Prometheus + Alertmanager)。我在基础架构、数据和运维方面进行实际操作。

## 我喜欢的工作方式

- **简明扼要，直截了当。** 行动胜于解释。如果你能删减字词而不丢失含义，请删减。
- **执行，不要叙述。** 展示结果，而不是一步一步地解说。
- **遵守 `CLAUDE.md` 中的铁律 (Ironclad Rules)** — 它们是经过惨痛教训学到的不可谈判的约定。绝不要违反它们；如果请求可能导致违反，请发出警告。
- **开门见山给出答案**，如果需要，再提供支持细节。
- **在报告和文档中优先使用散文体而不是列表泛滥**；仅在必要时才使用项目符号。
- **在声称完成之前进行验证** — 测试配置，验证 JSON，检查 SQL 类型转换。

## 我正在为此工作区优化的内容

1. **运维与可靠性** — 运行手册、事件响应、告警调优、安全部署
2. **文档与报告** — 状态报告、架构文档、检查点
3. **数据与仪表板** — TimescaleDB SQL、遥测分析、Grafana 工作
4. **待办事项交付** — 交付 `TASKS.md` 中的未结项

## 默认设置与护栏

- **安全模式:** 在执行破坏性/不可逆的更改（编辑 `node-red/flows/*.json`、仪表板 JSON、alertmanager 配置、数据库写入、git push、`docker compose` 重启）之前请先询问。只读分析无需确认。
- **绝对不要** 使用 PowerShell `ConvertTo-Json` 编辑 flow JSON (会损坏 `\n`)。
- **绝对不要** 使用 schema `ims.*` — 仅使用 `public`。
- 将 `secrets/` 和 `.env` 视为禁区；切勿打印或提交其内容。

## 惹人厌烦的事 (Pet peeves)

过长的开场白、“Let me…” 这样的填充词、重复解释我已经知道的事情，以及绕过铁律的不安全捷径。
