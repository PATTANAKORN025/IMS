<!-- GLOBAL_NAV -->
<div align="right">
  <a href="README.md"><img src="docs/assets/icons/home.svg" width="16" align="center" /> <b>Home</b></a> &nbsp;|&nbsp;
  <a href="docs/README.md"><img src="docs/assets/icons/book.svg" width="16" align="center" /> <b>Docs Index</b></a>
</div>
<br/>

# Production Readiness

Generated by `scripts/production-assurance.js` -- every cell below is computed from a real run's
machine-readable results (`docs/evidence/runtime/production-assurance-*.json`), never hand-typed.
The verdict is `scripts/gate.js`'s pure function over that JSON, not an assertion.

**Profile run:** `dr`  
**Timestamp:** 2026-08-21T05:53:19.903Z  
**Verdict:** **CONDITIONAL GO**

**Reasons:**
- CONDITIONAL GO: resilience.container-loss.recovery-time -- BLOCKED_SCOPE, blocking, cannot confirm PASS (requires --allow-container-kill (and, if run through production-assurance.js, an explicit --allow-container-kill CLI flag) -- an automated runner must never kill a real running container without this opt-in)
- CONDITIONAL GO: resilience.container-loss.detected -- BLOCKED_SCOPE, blocking, cannot confirm PASS (requires --allow-container-kill (and, if run through production-assurance.js, an explicit --allow-container-kill CLI flag) -- an automated runner must never kill a real running container without this opt-in)

| Requirement | Test | Threshold | Evidence | Result | Blocking | Next Action |
|---|---|---|---|---|---|---|
| Backup/restore actually works | disaster-recovery.backup-restore | 12/12 structural comparisons pass (table/schema/hypertable/index/FK/trigger/extension/CAGG/compression/retention/row-count/app-connectivity), isolated ims_dr_test only | docs/evidence/runtime/disaster-recovery-2026-08-21T05-51-49-612Z.log | PASS | yes | - |
| Failure recovery time | resilience.container-loss.recovery-time | container reaches running again within 120s of docker kill | n/a | BLOCKED_SCOPE | yes | intentionally withheld for safety; re-run with the required opt-in flag if this check is needed |
| Failure detection & alerting | resilience.container-loss.detected | ServiceDown alert fires (Alertmanager) within 90s of the induced failure | n/a | BLOCKED_SCOPE | yes | intentionally withheld for safety; re-run with the required opt-in flag if this check is needed |

A profile only exercises its own category list (see `scripts/production-assurance.js`'s `PROFILES`) -- a category not run this time shows as `NOT_TESTED` above and does not affect this run's verdict. Only a `full` profile run claims whole-system readiness.
