<!-- GLOBAL_NAV -->
<div align="right">
  <a href="../../README.md"><img src="../../docs/assets/icons/home.svg" width="16" align="center" /> <b>Home</b></a> &nbsp;|&nbsp;
  <a href="../../docs/README.md"><img src="../../docs/assets/icons/book.svg" width="16" align="center" /> <b>Docs Index</b></a>
</div>
<br/>

# IMS Alarm & Alert Playbook

> **Audience:** SRE/Operations, Factory Floor Operators.
> **Objective:** First-responder resolution steps mapped to the specific alerts and alarm codes generated by the system.
> **Provenance:** Every code and rule name below was checked directly against the live database and the provisioned alert rule files on 2026-08-10.
>
> IMS has **three distinct alerting layers**, and they use different vocabularies — don't conflate them:
>
> 1. **LDI machine alarm codes** — numeric codes from the physical/simulated equipment itself, stored in `public.ldi_alarm_log`, catalogued in `public.ldi_alarm_ms_code`. See `ALARM_SEVERITY_GUIDE.md` for the full taxonomy.
> 2. **Grafana native alert rules** — threshold/anomaly rules Grafana itself evaluates against the database, split into LDI-specific (`monitoring/grafana/provisioning/alerting/ldi-rules.yml`) and infrastructure (`rules.yml`).
> 3. **Prometheus/Alertmanager rules** — meta-monitoring of the platform itself (`monitoring/prometheus/rules/ims-alerts.yml`) — is IMS's own pipeline healthy, not "is a machine out of spec."

All three route through the same delivery path: Grafana's native alerts fire directly; Prometheus alerts go through Alertmanager → Node-RED's `/alert-webhook` (`nodered_data/flows/alerting.json`), which formats a LINE Messaging API and MS Teams payload. **Real delivery to LINE/Teams requires `LINE_CHANNEL_ACCESS_TOKEN` and `TEAMS_WEBHOOK_URL`, which are external configuration parameters.** The formatting and delivery logic is verified and robust, logging status upon failure. Messages will be routed upon configuration of credentials per `docs/admin/ADMIN_MANUAL.md`.

---

## 1. LDI Machine Alarm Codes

The 19 codes below are actively generated by the system today. They are categorized by subsystem to provide clear diagnostic workflows.

### 1.1 Vision & Alignment Subsystem

| Code       | Severity | Message                                          | First Response (L1)                                                                  | Engineering Workflow (L2/L3)                                                 |
| ---------- | -------- | ------------------------------------------------ | ------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------- |
| `01060009` | Major    | Wrong camera serial number                       | Check physical camera connection. Do NOT attempt to unplug/replug while power is on. | Verify serial config on the machine controller. Re-initialize vision module. |
| `0106000C` | Major    | Failed to stop camera                            | Power-cycle the camera module via the operator console.                              | Check frame grabber logs. Hard reset vision subsystem.                       |
| `90001`    | Warning  | Inner layer alignment to grip point failed       | Check if panel is loaded skewed. Reload panel.                                       | Check grip point tolerances in recipe. Recalibrate alignment targets.        |
| `90004`    | Warning  | Outer layer alignment to grip point failed       | Check if panel is loaded skewed. Reload panel.                                       | Same as 90001.                                                               |
| `90005`    | Warning  | Registration error (PE/JE) out of tolerance      | Pause loading. Open [LDI Engineering Analytics] to check Cpk.                        | Execute SPC OCAP (See `LDI_SPC_GUIDE.md`).                                   |
| `90012`    | Warning  | Alignment failed and operator cancelled exposure | Note in shift log. Verify next panel loads correctly.                                | Investigate if operator is habitually canceling alignment to meet quota.     |

### 1.2 Network & Communication Subsystem

| Code       | Severity | Message                    | First Response (L1)                                      | Engineering Workflow (L2/L3)                                      |
| ---------- | -------- | -------------------------- | -------------------------------------------------------- | ----------------------------------------------------------------- |
| `01060013` | Major    | Found the same IP          | Stop the line. This is an IT issue.                      | Scan subnet for duplicate IPs. Isolate conflicting device.        |
| `01100001` | Major    | Failed to connect to PLC   | Check the PLC's power indicator and ethernet link light. | Ping PLC from edge router. Reboot PLC switch port.                |
| `01130002` | Major    | Communication abnormality  | Note the exact time and machine state. Escalate.         | Check controller logs for serial/ethernet drops. Inspect cabling. |
| `92013`    | Warning  | Network connection timeout | Ping machine from NOC.                                   | Check core switch uplinks.                                        |

### 1.3 Motion & Mechanical Subsystem

| Code       | Severity | Message                                         | First Response (L1)                                             | Engineering Workflow (L2/L3)                                                   |
| ---------- | -------- | ----------------------------------------------- | --------------------------------------------------------------- | ------------------------------------------------------------------------------ |
| `010E0064` | Major    | Motor type undefined                            | Do not attempt to move axes manually. Escalate.                 | Verify motor type is set in the machine's recipe/config. Reload config.        |
| `10006`    | Major    | Failed to set imaging device to protection mode | **DO NOT RESUME EXPOSURE.** Escalate immediately.               | Manually engage protection mode via maintenance interface. Inspect interlocks. |
| `0106001C` | Minor    | Stop trigger wait signal timeout                | Watch for repeat occurrences. Log if it happens >3 times/shift. | Check timing/signal logic on the stop trigger. Clean sensor heads.             |
| `70004`    | Warning  | Position-synchronised output overspeed          | Check recipe scan speed. Is it unusually high?                  | See `LDI_RCA_GUIDE.md` MOTION category. Recalibrate encoder.                   |

### 1.4 Environmental & System

| Code    | Severity | Message                                  | First Response (L1)                                     | Engineering Workflow (L2/L3)                                                    |
| ------- | -------- | ---------------------------------------- | ------------------------------------------------------- | ------------------------------------------------------------------------------- |
| `80001` | Warning  | Waiting for subdrawing prep data timeout | Check MES/data feed for that job. Wait 5 minutes.       | Investigate CAM/MES data generation pipeline lag.                               |
| `91008` | Warning  | Ambient temperature or humidity abnormal | Check cleanroom HVAC panel.                             | See `LDI_RCA_GUIDE.md` THERMAL category. Cross-reference with facility sensors. |
| `91009` | Warning  | Vacuum pressure out of control range     | Ensure panel is flat on the platen. Clean vacuum holes. | See `LDI_RCA_GUIDE.md` VACUUM category. Check pump seals.                       |
| `93004` | Warning  | Calibration cycle exception              | Do not process production panels until recalibrated.    | Manually trigger calibration cycle and read logs.                               |
| `97005` | Warning  | Database connection exception            | The machine's own local DB is down. Escalate.           | Check machine controller local storage.                                         |

---

## 2. Authorization Boundaries & Resolution Workflow

**Who can clear an alarm?**

- **Operator (L1):** Authorized to clear `Warning` and `Minor` alarms _if_ the physical root cause is resolved (e.g., clearing a jammed panel).
- **Engineering (L2/L3):** Must clear all `Major` and `Critical` alarms. Operators must not override these.

**Verification of Resolution:**
An alarm is not "resolved" simply because the alarm bell stops ringing.

1. **Clear the physical fault** on the machine.
2. **Acknowledge in UI:** Use the `IMS LDI - Alarm Console` to mark the alarm as "Acknowledged", then "Resolved".
3. **Verify Telemetry:** Open the `Operator Andon Board`. Verify the machine has returned to a Green state for at least 3 consecutive polling cycles (15 seconds).

**Where to see these live:** the `IMS LDI - Operator Andon Board`'s Action Queue panel (read-only, 5s refresh) shows Critical/Major alarms not yet resolved.

---

## 2. Grafana Native Alert Rules — LDI (`ldi-rules.yml`)

| Rule                                             | What it means                                                                                                                                                      | Investigate                                                                                        |
| ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------- |
| **LDI Quality Drift — Max PE exceeds tolerance** | A machine's worst PE (position error) sample breached its `pe_setting` tolerance.                                                                                  | `LDI Engineering Analytics` → SPC panels for the affected machine.                                 |
| **LDI Process Capability — Cpk below 1.33**      | PE-based Cpk dropped below the 1.33 industry-standard capability floor.                                                                                            | Same dashboard; check whether it's a single excursion or sustained drift — see `LDI_SPC_GUIDE.md`. |
| **LDI Quality Drift — Max JE exceeds tolerance** | Same as PE drift, for JE (judgment error).                                                                                                                         | Same dashboard, JE panels.                                                                         |
| **LDI JE Process Capability — Cpk below 1.33**   | JE-based Cpk below 1.33.                                                                                                                                           | Same dashboard.                                                                                    |
| **LDI Machine Alarm — new alarm in database**    | A new row landed in `ldi_alarm_log`. This is the catch-all "something happened" rule — check §1 above for the specific code.                                       | Operator Andon Board.                                                                              |
| **LDI Temperature High — above 24°C spec limit** | Fixed-threshold rule, not a Z-Score anomaly — see `LDI_RCA_GUIDE.md`'s note that there is no statistical-anomaly panel on the LDI side, only this fixed threshold. | `LDI Engineering Analytics` temperature panel.                                                     |

## 3. Grafana Native Alert Rules — Infrastructure (`rules.yml`)

| Rule                                                            | Scope                                                                                                                                                                                                                                                              |
| --------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| High CPU / RAM / Disk Usage                                     | Server/workstation resource thresholds.                                                                                                                                                                                                                            |
| High Temperature                                                | Server chassis temperature.                                                                                                                                                                                                                                        |
| High Network Error Rate / Network Packet Drops / Interface Down | Network interface health.                                                                                                                                                                                                                                          |
| Bandwidth Saturation Forecast                                   | Predictive — capacity trending toward saturation, not yet breached.                                                                                                                                                                                                |
| CPU Z-Score Anomaly (>3σ) / Temperature Z-Score Anomaly (>3σ)   | Statistical anomaly detection — infrastructure only. There is **no equivalent Z-Score panel on the LDI side** (see `LDI Temperature High` above, which is a fixed threshold instead).                                                                              |
| LDI Vibration Critical                                          | **Paused.** `ldi_metrics.vibration` streams as `0` for LDI devices (a system constraint in the legacy `ldi_metrics` pipeline, not `ldi_data` — see `ARCHITECTURE.md`'s System Constraints & Technical Boundaries). This rule is paused pending future integration. |

Investigate infrastructure alerts starting at `NOC Overview` or `Engineering Drill-Down`.

## 4. Prometheus / Alertmanager Rules (`ims-alerts.yml`)

These monitor **IMS's own pipeline health**, not machine or server state — "is the monitoring system itself working."

| Rule                                                                      | Meaning                                                                                                                                            | First response                                                                                                                                                             |
| ------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `PrometheusDown` / `AlertmanagerDown` / `TargetDown` / `HighScrapeErrors` | The monitoring stack's own components are unhealthy.                                                                                               | Check `docker ps` for the affected container; see `INCIDENT_RESPONSE.md`.                                                                                                  |
| `ServiceDown` / `ServiceHighLatency` / `SLABreachWarning`                 | Blackbox-exporter probe failures against a monitored endpoint (e.g. Grafana's own `/api/health`).                                                  | Check the target service directly; note this can be a transient artifact of a deliberate container recreate — see `DR_TEST_PLAN.md`'s Drill 2 evidence for a real example. |
| `SSLCertExpiring`                                                         | A monitored endpoint's TLS cert is nearing expiry.                                                                                                 | Renew before expiry.                                                                                                                                                       |
| `Watchdog`                                                                | Always-firing heartbeat confirming Alertmanager routing itself works — **not** a real incident. Exclude it when scanning for actual firing alerts. | N/A - Working as intended                                                                                                                                                  |
| `PipelineDataStalled` / `PipelineDataDegraded` / `PipelineHighErrorRate`  | Node-RED ingestion pipeline health — stalled/degraded/high-error-rate telemetry flow.                                                              | `Meta-Monitoring` dashboard; check `ims-node-red` logs for the specific failure signature (see `INCIDENT_RESPONSE.md`'s worked examples).                                  |
| `CircuitBreakerOpen`                                                      | A device's SNMP circuit breaker tripped (CLOSED→OPEN after repeated failures).                                                                     | Check the specific device's connectivity; the breaker self-resets to HALF_OPEN after a cooldown.                                                                           |

## Related documents

- `docs/operations/INCIDENT_RESPONSE.md` — escalation procedures if an alert indicates a platform-level incident.

---

[⬅️ Back to IMS Platform Book](../architecture/IMS_PLATFORM_BOOK.md) | [🏠 Main Repository](../../README.md)

_Version 2.0, corrected 2026-08-10 — see `docs/architecture/IMS_MANUFACTURING_PLATFORM_V2.md` for the broader documentation program this fix was part of._
