# Dashboard Inventory

> **ไฟล์ที่ถูกสร้างขึ้นอัตโนมัติ — ห้ามแก้ไขด้วยตนเอง** สร้างใหม่ด้วย:
> `node scripts/generate-dashboard-inventory.js`
>
> แหล่งข้อมูลหลัก: `monitoring/grafana/dashboards/{infrastructure,manufacturing}/*.json` (title, uid, panel
> count, description — ทั้งหมดอ่านโดยตรงจาก JSON ไม่มีการพิมพ์ด้วยตนเอง)
> การนับจำนวน panel ใช้การคำนวณที่เหมือนกันกับ
> `tests/lint/dashboard-linter.js` (`data.panels.length`) ดังนั้นไฟล์นี้และ
> ผลลัพธ์ในคอนโซลของ linter จะไม่มีทางขัดแย้งกัน การตรวจสอบของ CI
> (`node scripts/generate-dashboard-inventory.js --check`) จะทำให้การบิลด์ล้มเหลว
> หากไฟล์นี้ไม่ตรงกับสิ่งที่แดชบอร์ดระบุไว้ในปัจจุบัน
>
> สร้างล่าสุด: 2026-08-18 | แดชบอร์ดทั้งหมด: 15 | พาเนลทั้งหมด: 190

## Infrastructure (5)

| UID                     | Title                                 | Panels | Purpose                                                                                                                                                                                                                  |
| ----------------------- | ------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `ims-capacity`          | IMS AIOps & Capacity Forecast         | 16     | คาดการณ์จำนวนวันก่อนที่จะเต็ม/จุดอิ่มตัวสำหรับ CPU, RAM และดิสก์ ผ่านการวิเคราะห์การถดถอยเชิงเส้น 30 วัน (30-day linear regression) พร้อมการตรวจจับความผิดปกติด้วย Z-Score (>3sigma) เน้นที่โครงสร้างพื้นฐาน             |
| `ims-engineering`       | IMS Engineering Drill-Down            | 25     | เจาะลึกระดับเซิร์ฟเวอร์: เกจและอนุกรมเวลาของ CPU/RAM/ดิสก์/อุณหภูมิ/เครือข่ายสำหรับเครื่องที่เลือก พร้อมทั้งพาเนลปริมาณงาน/คุณภาพของ LDI จากไปป์ไลน์แบบเก่าและพาเนลตรวจจับความผิดปกติด้วย Z-Score                        |
| `ims-ingestion-latency` | IMS Ingestion Latency                 | 13     | อ่านอย่างเดียว หลักฐานความหน่วงจริงจาก source_ts -> ingest_ts จากคอลัมน์ ingest_ts ของการย้ายข้อมูล 081 -- ไม่มีข้อมูลจำลอง ไม่มีการดำเนินการเขียนแบบตอบโต้ เป็นส่วนเสริมของ tests/e2e/ingestion-latency-check.js        |
| `ims-meta-monitoring`   | IMS Pipeline Health & Meta-Monitoring | 16     | สุขภาพของไปป์ไลน์การนำเข้าข้อมูลเอง: อัตราการแทรกข้อมูลเป็นแถว/วินาที, อัตราความสำเร็จของแบทช์, ความลึกของคิวที่รอการลองใหม่, สถานะเซอร์กิตเบรกเกอร์ และอัตราการโพลอุปกรณ์ เฝ้าดูไปป์ไลน์ ไม่ใช่กลุ่มอุปกรณ์ที่มันติดตาม |
| `ims-noc-overview`      | IMS NOC Overview                      | 7      | โครงสร้างพื้นฐานเท่านั้น (เซิร์ฟเวอร์) -- เมตริกกระบวนการ/คุณภาพของ LDI จะอยู่บนแดชบอร์ด Manufacturing และ Machine Snapshot                                                                                              |

## LDI Manufacturing (10)

| UID                             | Title                                                 | Panels | Purpose                                                                                                                                                                                                                                       |
| ------------------------------- | ----------------------------------------------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ims-easy-overview`             | IMS Easy Overview                                     | 8      | วิธีที่ง่ายที่สุดในการดูกลุ่มอุปกรณ์ LDI ทั้งหมดพร้อมกัน: ไม่ต้องตั้งค่าตัวแปรเทมเพลต ไม่ต้องกำหนดค่าตัวกรอง แค่เปิดขึ้นมา สร้างจากวิว/ฟังก์ชันที่แชร์กันในรีโพสิทอรีนี้ทั้งหมด (v_ldi_machine_latest_full, v_ldi_alarm_c...)                 |
| `ims-ldi-alarm-console`         | IMS LDI - Alarm Console                               | 2      | เวิร์กโฟลว์การรับทราบ/แก้ไขการเตือนแบบตอบโต้ -- เขียนสถานะจริงลงใน public.ldi_alarm_lifecycle เป็นส่วนเสริมของแดชบอร์ดอ่านอย่างเดียว IMS LDI - Operator Andon Board (ตู้หน้าจอทีวีติดผนัง ไม่มีองค์ประกอบแบบตอบโต้)                           |
| `ims-ldi-alarm-dictionary`      | IMS LDI - Alarm Dictionary                            | 3      | แดชบอร์ดค้นหาข้อมูลอ้างอิง: คำจำกัดความ Alarm Master ฉบับเต็มจากผู้ผลิต + การเกิดจริงล่าสุดสำหรับ Alarm Code ใดๆ ไม่ได้เป็นส่วนหนึ่งของโฟลว์การนำทางสำหรับผู้ปฏิบัติงาน/วิศวกร -- เปิดผ่านลิงก์เจาะลึกจากคอลัมน์ Alarm Code...                |
| `ims-ldi-alarm-response`        | IMS LDI - Alarm Response (MTTA/MTTR)                  | 8      | ทีมตอบสนองต่อการเตือนเร็วพอหรือไม่? MTTA/MTTR จริงจาก public.ldi_alarm_lifecycle -- ไม่มีข้อมูลจำลอง กลุ่มเป้าหมายคือหัวหน้ากะ / เจ้าของการผลิต เช่นเดียวกับ Manufacturing Command Center                                                     |
| `ims-ldi-engineering-analytics` | IMS LDI - Engineering Analytics & SPC                 | 16     | เลเยอร์ 3 ไทม์ไลน์กระบวนการ: RCA แบบหลายพารามิเตอร์ที่ซิงโครไนซ์กัน temperature → humidity → scan_speed → air_vacuum → scale_x/y → pe_1~~6 → je_1~~4 → state ครอสแฮร์ที่ใช้ร่วมกัน + ทูลทิป (tooltip) การปรับสเกลแกนแบบคงที่                  |
| `ims-ldi-factory-digital-twin`  | IMS LDI - Factory Digital Twin                        | 1      | TASK 3 -- แฝดดิจิทัลของโรงงานแบบ Canvas จำนวน 10 เครื่องเต็มรูปแบบ ขยายจาก POC จำนวน 2 เครื่องใน Task 2 แสดงเครื่อง LDI ที่รายงานผลจริงทั้ง 10 เครื่อง (LDI-01..LDI-10) ที่จัดกลุ่มตามโซนจริงทั้ง 5 โซน (public.devices.location), 2 เครื่... |
| `ims-ldi-machine-snapshot`      | IMS LDI - Machine Snapshot                            | 14     | สแนปชอตของเครื่องแบบ 360° ณ เสี้ยววินาทีที่คลิกจากไทม์ไลน์กระบวนการ แสดงบริบทของงาน, ตัวแปรทางกายภาพ, การจัดตำแหน่ง PE, Cpk และความใกล้เคียงของการเตือน                                                                                       |
| `ims-ldi-manufacturing`         | IMS LDI - Manufacturing Command Center                | 33     | แดชบอร์ด RCA 4 เลเยอร์: HUD สำหรับผู้บริหาร + ข้อมูลโทรมาตรของเครื่องจักร + บริบทการผลิต + สตรีมการเตือน การตั้งชื่อตามสคีมา (Schema-driven) ครอสแฮร์ที่ใช้ร่วมกัน การปรับสเกลแกนแบบคงที่                                                     |
| `ims-ldi-operator-andon`        | IMS LDI - Operator Andon Board                        | 11     | ตู้บนพื้นที่โรงงาน สอดคล้องกับมาตรฐาน ISA-101 ไม่มีการตอบโต้ ไม่มีการเลื่อนหน้าจอ ความละเอียด 1280x720 ออกแบบใหม่จากเลย์เอาต์ 1920x1080 ก่อนหน้านี้ (System Audit Phase 5): ตัวเลือกตัวแปรเทมเพลตและแถวลิงก์เจาะลึกถูกซ่อนไว้...              |
| `ldi-data-readiness`            | LDI Data Readiness & Integration Gaps (Real Database) | 17     | แดชบอร์ดความพร้อมตามหลักฐานที่อิงจากแถว PostgreSQL ปัจจุบันเท่านั้น ไม่มีข้อมูลจำลอง                                                                                                                                                          |
